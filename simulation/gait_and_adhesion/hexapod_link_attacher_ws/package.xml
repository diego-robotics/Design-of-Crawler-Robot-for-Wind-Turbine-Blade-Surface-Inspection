<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>hexapod_link_attacher</name>
  <version>0.0.1</version>
  <description>
    Namespaced Gazebo Classic link-attacher plugin. Same AttachLink/DetachLink
    service interface as IFRA_LinkAttacher, but supports multiple independent
    instances (one per namespace) so several simultaneous rigid attachments
    can exist at once -- needed for a hexapod's tripod gait, where three legs
    must be attached at the same time.
  </description>
  <maintainer email="you@example.com">diego</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>gazebo_dev</depend>
  <depend>gazebo_ros</depend>
  <depend>rclcpp</depend>
  <depend>linkattacher_msgs</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
